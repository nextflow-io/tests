machine:
  java:
    version: openjdk7
  services:
    - docker
    
test:
  override:
    - curl -fsSL get.nextflow.io | bash
    - docker pull nextflow/examples
    - export WITH_DOCKER=yes; export NXF_CMD=$PWD/nextflow; cd tests && bash run.sh 
    